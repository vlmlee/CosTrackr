import React, { Component, Proptypes } from 'react';

export default class ProjectEdit extends Component {
	render() {
		return (
			<div> This is where the project edit should go. It should be the code currently found in Project.js.
			</div>
		);
	}
}